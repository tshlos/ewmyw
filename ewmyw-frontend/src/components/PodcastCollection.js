import React, { Component } from "react";

const PodcastCollection = (props) => {

    // const { name, description, url, image, publisher, total_episodes, category, show_id } = props.podcast
    return (
        <div className="card">
            {/* <h1> Podcast Collection </h1>
            <h5> {name} </h5>
            <h5> {description} </h5>
            <h5> {publisher} </h5> */}

            {/* {props.podcasts.map(podcast => {
                return (
                    <div> 
                        podcast={podcast}
                    </div>
                )
            })} */}
        </div>
    )
}
export default PodcastCollection